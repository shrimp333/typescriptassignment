// TODO: create a SpinRecord class which implements ISpin and adds a new attribute num:number
export class SpinRecord {
    constructor(colour, bodyPart) {
        this.colour = colour;
        this.bodyPart = bodyPart;
        this.num = 0;
        this.colour = colour;
        this.bodyPart = bodyPart;
    }
}
